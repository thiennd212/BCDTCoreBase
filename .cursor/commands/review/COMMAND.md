# /review – Rà soát nhanh

1. Build BE (tắt BCDT.Api trước) + FE nếu có. Ghi Pass/Fail.
2. `git status` + `git diff --stat` → tóm tắt file đã sửa.
3. Gợi ý: sửa API → chạy /bcdt-verify; sửa FE → DevTools + tiếng Việt (bcdt-frontend).

**Tự động:** Thực hiện không hỏi.
